spring:
  application:
    name: creditCardManagement
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/credit_card_management?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:myheadpounds16D}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    allow-circular-references: true
  jackson:
    serialization:
      write-dates-as-timestamps: false
    date-format: yyyy-MM-dd'T'00:00:00
logging:
  level:
    org:
      springframework: DEBUG
app:
  jwt:
    secret: ${JWT_SECRET:SomeSecretKeyoidhujh9jkoi9ydhuj534nh89jjnhs7xvjmhziklogjmmskmrpojksgns8ejHUYhfnnbythbfhjz7lUHNN89f79gin58ujmgnbn}
    tokenExpiration: ${JWT_TOKEN_EXPIRATION:300m}
    refreshTokenExpiration: ${JWT_REFRESH_TOKEN_EXPIRATION:30m}
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html